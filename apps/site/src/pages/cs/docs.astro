---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Dokumentace | Blood Kings Monitoring" description="Oficiální dokumentace k instalaci, konfiguraci a správě Blood Kings Monitoring a monitorovacích agentů.">
  <div class="docs-layout-container container">
    <!-- Sidebar Doc Links -->
    <aside class="docs-sidebar">
      <div class="docs-sidebar-section">
        <p class="sidebar-heading">Začínáme</p>
        <a href="#quickstart" class="sidebar-link active">Rychlý start</a>
        <a href="#requirements" class="sidebar-link">Požadavky</a>
        <a href="#architecture" class="sidebar-link">Architektura</a>
      </div>
      <div class="docs-sidebar-section">
        <p class="sidebar-heading">Centrální Server</p>
        <a href="#server-config" class="sidebar-link">Konfigurace databáze</a>
        <a href="#server-env" class="sidebar-link">Proměnné prostředí</a>
        <a href="#server-status" class="sidebar-link">Status stránky</a>
      </div>
      <div class="docs-sidebar-section">
        <p class="sidebar-heading">Spolehlivost</p>
        <a href="#heartbeat" class="sidebar-link">Heartbeat monitory</a>
        <a href="#watchdog" class="sidebar-link">Hlídač sběru dat</a>
      </div>
      <div class="docs-sidebar-section">
        <p class="sidebar-heading">Agenti</p>
        <a href="#agent-install" class="sidebar-link">Registrace agenta</a>
        <a href="#agent-config" class="sidebar-link">Konfigurace intervalu</a>
        <a href="#agent-trouble" class="sidebar-link">Řešení potíží</a>
      </div>
    </aside>

    <!-- Docs Content Area -->
    <div class="docs-main-content">
      <header class="docs-head">
        <h1>Dokumentace</h1>
        <p class="docs-intro">Instalace serveru, přidání monitorů a připojení agentů.</p>
      </header>
      <!-- Section 1 -->
      <section id="quickstart" class="docs-section">
        <span class="badge badge-accent">Příručka</span>
        <h2>Rychlý start</h2>
        <p>Tento průvodce vás provede od prázdného PHP hostingu k běžícímu serveru Blood Kings Monitoring.</p>
        
        <h3>Krok 1: Spuštění Serveru</h3>
        <p>Server je čisté PHP s databází MySQL nebo MariaDB - žádný obraz se nestahuje. Stáhněte kód, doplňte připojení k databázi a naimportujte schéma:</p>
        <div class="docs-code-wrapper">
          <pre><code>git clone https://github.com/BKPepe/monitoring.git &amp;&amp; cd monitoring/apps/status
cp config.sample.php config.php    # DB_HOST, DB_NAME, DB_USER, DB_PASS
mysql -u YOUR_DB_USER -p YOUR_DB_NAME &lt; schema.sql</code></pre>
        </div>
        <p>Obsah <code>apps/status/</code> nahrajte do <code>public_html/status/</code> a sběr dat spouštějte z cronu každou minutu:</p>
        <div class="docs-code-wrapper">
          <pre><code>* * * * * php -q /path/to/status/cron.php &gt;/dev/null 2&gt;&amp;1</code></pre>
        </div>
        <p>Přehled na <code>/app</code> je samostatný frontend: jednou ho sestavte příkazem <code>npm ci &amp;&amp; npm run build:monitor</code> a obsah <code>apps/monitor/dist/</code> nahrajte do <code>public_html/app/</code>. Pak na své doméně otevřete <code>/app/setup</code> a založte prvního administrátora.</p>

        <h3>Krok 2: Přidání nového monitoru</h3>
        <p>Po přihlášení do administrace přejděte na záložku <strong>Monitors</strong> a klikněte na <strong>Add Monitor</strong>. Můžete přidat HTTPS kontroly, kontroly TCP portů, kontroly SSL certifikátů nebo <a href="#heartbeat">heartbeat</a> pro úlohy, které se hlásí samy.</p>
      </section>

      <!-- Section 2 -->
      <section id="requirements" class="docs-section">
        <hr class="docs-divider" />
        <h2>Systémové požadavky</h2>
        <p>Blood Kings Monitoring je navržen tak, aby běžel na jakémkoliv hardwaru.</p>
        
        <h4>Centrální Server:</h4>
        <ul>
          <li><strong>Procesor:</strong> 1 vCPU (x86_64 nebo ARM64)</li>
          <li><strong>Operační paměť:</strong> 128 MB RAM (doporučeno 256 MB)</li>
          <li><strong>Místo na disku:</strong> aplikace a databáze MySQL/MariaDB, která roste s historií kontrol</li>
          <li><strong>Software:</strong> PHP 8.2+ (CI běží na 8.4) s webovým serverem (Apache/Nginx) a MySQL nebo MariaDB. Server běží bez Dockeru; možnost Dockeru má jen Python agent.</li>
        </ul>

        <h4>Vzdálený Agent:</h4>
        <ul>
          <li><strong>OS:</strong> Linux (libovolná distribuce s Bash a Curl), Windows 10/11/Server, OpenWrt/Turris OS, Raspberry Pi OS</li>
          <li><strong>Paměť:</strong> méně než 10 MB RAM</li>
          <li><strong>Síť:</strong> Odchozí HTTPS (port 443) přístup k centrálnímu serveru</li>
        </ul>
      </section>

      <!-- Section 3 -->
      <section id="architecture" class="docs-section">
        <hr class="docs-divider" />
        <h2>Architektura systému</h2>
        <p>Blood Kings Monitoring využívá hybridní architekturu spojující aktivní piny a pasivní agenty.</p>
        
        <div class="architecture-diagram card">
          <div class="arch-block">
            <strong>Vzdálený Agent</strong>
            <span>(Linux/Windows/Docker)</span>
            <span class="arrow">↓ HTTPS POST (Metrics)</span>
          </div>
          <div class="arch-block highlight">
            <strong>Centrální Server</strong>
            <span>(PHP / MySQL nebo MariaDB)</span>
            <span class="arrow">↓ Render</span>
          </div>
          <div class="arch-block">
            <strong>Veřejná Status Stránka</strong>
            <span>(HTML / cached JSON)</span>
          </div>
        </div>

        <p>
          <strong>Pasivní monitoring (Agenti):</strong> Agenta na hlídaném stroji spouští cron (nebo Plánovač úloh): na OpenWrt každou minutu, jinde standardně každých 5 minut. Každý běh přečte lokální data (CPU, RAM, disky, síť) a zasílají je pomocí HTTPS POST požadavků na centrální server. Tento přístup nevyžaduje otevírání žádných příchozích portů na monitorovaných serverech.
        </p>
        <p>
          <strong>Aktivní monitoring (Pinger):</strong> Cron centrálního serveru kontroluje cíle přímo: požadavky HTTP/HTTPS (s volitelným klíčovým slovem a expirací certifikátu) a TCP spojení. ICMP ping ani kontroly UDP neumí; „ping“ je TCP spojení.
        </p>
      </section>

      <!-- Central Server Sekce -->
      <section id="server-config" class="docs-section">
        <hr class="docs-divider" />
        <h2>Konfigurace Databáze Serveru</h2>
        <p>Blood Kings Monitoring se připojuje k MySQL nebo MariaDB databázi pomocí PDO. Údaje nastavte v souboru <code>status/config.php</code>:</p>
        <div class="docs-code-wrapper">
          <pre><code>define('DB_HOST', '127.0.0.1');
define('DB_NAME', 'bloodkings_status');
define('DB_USER', 'bloodkings_user');
define('DB_PASS', 'VaseSilneHeslo123!');
define('TIMEZONE', 'Europe/Prague');</code></pre>
        </div>
      </section>

      <section id="server-env" class="docs-section">
        <hr class="docs-divider" />
        <h2>Proměnné Prostředí a Tokeny</h2>
        <p>Klíčové bezpečnostní tokeny se definují přímo v <code>config.php</code> nebo v prostředí serveru:</p>
        <ul>
          <li><code>cron_key</code>: Tajný klíč pro autorizaci spouštění <code>cron.php</code> z externích služeb a webhooků.</li>
          <li><code>agent_registration_token</code>: Sdílené heslo autorizující automatickou auto-registraci nově nainstalovaných agentů.</li>
          <li><code>metrics_token</code>: Bearer token chránící Prometheus endpoint <code>metrics.php</code>.</li>
        </ul>
      </section>

      <section id="server-status" class="docs-section">
        <hr class="docs-divider" />
        <h2>Veřejné Status Stránky a Branding</h2>
        <p>Vzhled veřejných status stránek upravíte v Administraci (Nastavení -> Branding). Můžete nahrát vlastní logo, nastavit barvy a konfiguraci 24h / 7d / 30d SLA agregační souborové cache.</p>
      </section>

      <!-- Agent Sekce -->
      <section id="heartbeat" class="docs-section">
        <hr class="docs-divider" />
        <h2>Heartbeat monitory</h2>
        <p>Aktivní kontrola umí jen to, na co dosáhne ze sítě. Záloha, která se spustí ve tři ráno a tiše selže, je pro ni neviditelná - není co pingnout. Heartbeat obrací směr: úloha se hlásí sama a monitor spadne ve chvíli, kdy se neozve.</p>
        <p>Založte monitor typu <strong>Heartbeat</strong>, nastavte, jak často se má úloha ozvat, a na její konec přidejte adresu, kterou dostanete:</p>
        <div class="docs-code-wrapper">
          <pre><code>curl -fsS -m 10 "https://vas-server/status/heartbeat.php?token=VAS_TOKEN"</code></pre>
        </div>
        <p>Když úloha selže, ať to řekne rovnou - monitor pak spadne hned, ne až po vypršení intervalu:</p>
        <div class="docs-code-wrapper">
          <pre><code>curl -fsS -m 10 "https://vas-server/status/heartbeat.php?token=VAS_TOKEN&amp;status=fail&amp;msg=tar%20skoncil%20kodem%202"</code></pre>
        </div>
        <p>Monitor, který ještě nikdy nedostal signál, hlásí <strong>neznámý stav</strong>, ne výpadek. Nic neselhalo - jen o něm zatím nic nevíme, a upozornění na výpadek, který se nestal, je svého druhu lež.</p>
        <p>Token je jediné, co endpoint autorizuje. Nepouštějte ho do sdílených logů: kdo ho má, může se hlásit za vás a monitor bude svítit zeleně, i když záloha dávno neběží.</p>
      </section>

      <section id="watchdog" class="docs-section">
        <hr class="docs-divider" />
        <h2>Hlídač: kdo hlídá sběr dat</h2>
        <p>Všechno sbírá jediná cron úloha. Když umře, aplikace se nerozbije - dál zobrazuje poslední známé stavy a vypadá naprosto zdravě. Ze všech způsobů, jak může monitoring selhat, je tenhle nejhorší, protože o sobě nedá vědět.</p>
        <p>Server proto zaznamenává každý dokončený běh a vystavuje ho:</p>
        <div class="docs-code-wrapper">
          <pre><code>curl -s "https://vas-server/status/api.php?action=collection_health"</code></pre>
        </div>
        <p>Odpověď má <code>stale: true</code>, jakmile je poslední běh starší než nastavený limit - a také tehdy, když cron neběžel ještě ani jednou, protože „žádná data" není totéž co „vše v pořádku".</p>
        <p>Ten endpoint kontroluje Cloudflare Worker každých pět minut. Běží mimo hlídaný server záměrně: hlídač na stejném stroji umře spolu s tím, co hlídá. Stačí mu jednou nastavit Discord webhook a ozve se sám:</p>
        <div class="docs-code-wrapper">
          <pre><code>cd apps/worker &amp;&amp; npx wrangler secret put WATCHDOG_DISCORD_WEBHOOK</code></pre>
        </div>
        <p>Bez toho hlídač kontroluje dál, ale umí jen logovat - a přizná to na <code>/api/watchdog</code>, místo aby jen budil dojem, že něco hlídá.</p>
      </section>

      <section id="agent-install" class="docs-section">
        <hr class="docs-divider" />
        <h2>Instalace a Registrace Agenta</h2>
        <p>Agenti jsou samostatné skripty pro Linux (Bash), Python, PowerShell a OpenWrt routery. Bash a OpenWrt agent se zaregistrují sami: předejte jim registrační token z nastavení administrace a adresu <code>agent_api.php</code> vašeho serveru. Klíč, který agent dostane, uloží do <code>agent.cfg</code> vedle skriptu (na routeru do <code>agent_openwrt.cfg</code>).</p>
        <div class="docs-code-wrapper">
          <pre><code>./agent.sh --register VAS_REGISTRACNI_TOKEN https://bloodkings.eu/status/agent_api.php</code></pre>
        </div>
        <p>Oba argumenty jsou poziční; bez adresy se agent registruje proti výchozímu <code>localhost</code>. Python a PowerShell agent registrační příkaz nemají: do <code>agent.cfg</code> zapište <code>API_URL</code> a <code>AGENT_KEY</code> z nastavení monitoru. Celý postup pro každou platformu je na <a href="/cs/download#install-guide">stránce ke stažení</a>.</p>
      </section>

      <section id="agent-config" class="docs-section">
        <hr class="docs-divider" />
        <h2>Intervaly Agenta a Vzdálené Akce</h2>
        <p>Agenty spouští cron: na serverech každých 5 minut, na OpenWrt routerech každou minutu. Na OpenWrt routerech lze zapnout opt-in HMAC podepsané Vzdálené akce nastavením <code>REMOTE_ACTIONS_ENABLED=1</code> v konfiguraci agenta.</p>
      </section>

      <section id="agent-trouble" class="docs-section">
        <hr class="docs-divider" />
        <h2>Řešení Problémů s Agenty</h2>
        <p>Pokud agent přestane hlásit data, zkontrolujte odchozí přístup na port 443, pak ho spusťte ručně a přečtěte jeho log (leží vedle skriptu):</p>
        <div class="docs-code-wrapper">
          <pre><code># Jeden běh s úplným výpisem, pak poslední řádky logu
/opt/bk-agent/agent.sh --verbose
tail -n 50 /opt/bk-agent/agent.log</code></pre>
        </div>
      </section>
    </div>
  </div>
</Layout>

<style>
  .docs-layout-container {
    display: grid;
    grid-template-columns: 260px minmax(0, 1fr);
    gap: 4rem;
    padding-top: 4rem;
    padding-bottom: 6rem;
  }

  /* Sidebar Links */
  .docs-sidebar {
    position: sticky;
    top: calc(var(--header-height) + 2rem);
    height: fit-content;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .docs-sidebar-section {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .docs-sidebar-section .sidebar-heading {
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-hi);
    margin-bottom: 0.5rem;
  }

  .sidebar-link {
    font-size: 0.9rem;
    color: var(--text-mid);
    padding: 0.25rem 0;
    border-left: 2px solid transparent;
    padding-left: 1rem;
    margin-left: -2px;
  }

  .sidebar-link:hover {
    color: var(--text-hi);
  }

  .sidebar-link.active {
    color: var(--brand-500);
    font-weight: 600;
    border-color: var(--brand-500);
  }

  /* Content styles */
  .docs-main-content {
    display: flex;
    flex-direction: column;
    gap: 4rem;
  }

  .docs-head h1 {
    font-size: var(--text-4xl);
    font-weight: 700;
  }

  .docs-intro {
    margin-top: 0.75rem;
    font-size: var(--text-lg);
  }

  .docs-section h2 {
    font-size: 2rem;
    font-weight: 800;
    margin-top: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .docs-section h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .docs-section h4 {
    font-size: 1.15rem;
    font-weight: 700;
    margin-top: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .docs-section p {
    font-size: 1rem;
    margin-bottom: 1.2rem;
  }

  .docs-section ul {
    margin-bottom: 1.5rem;
    padding-left: 1.5rem;
  }

  .docs-section li {
    margin-bottom: 0.5rem;
    color: var(--text-mid);
  }

  .docs-divider {
    border: 0;
    height: 1px;
    background: var(--border);
    margin-bottom: 4rem;
  }

  /* Code wraps */
  .docs-code-wrapper {
    background: #09090b;
    border: 1px solid rgba(255, 255, 255, 0.05);
    padding: 1.2rem;
    border-radius: var(--radius-sm);
    margin: 1rem 0;
    overflow-x: auto;
  }

  .docs-code-wrapper code {
    color: #f4f4f5;
    font-family: var(--font-mono);
    font-size: 0.8rem;
  }

  /* Diagram styles */
  .architecture-diagram {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 2rem;
    align-items: center;
    background: var(--surface-1);
    border: 1px solid var(--border);
    margin: 1.5rem 0;
  }

  .arch-block {
    border: 1px solid var(--border);
    background: var(--surface-2);
    padding: 1rem 2rem;
    border-radius: var(--radius-sm);
    text-align: center;
    width: 280px;
    display: flex;
    flex-direction: column;
  }

  .arch-block strong {
    font-size: 0.95rem;
    color: var(--text-hi);
  }

  .arch-block span {
    font-size: 0.8rem;
    color: var(--text-low);
  }

  .arch-block.highlight {
    border-color: var(--brand-500);
    background: rgba(176,0,32,0.02);
  }

  .arrow {
    display: block;
    margin-top: 1rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--brand-500);
    font-weight: 600;
  }

  @media (max-width: 992px) {
    .docs-layout-container {
      grid-template-columns: minmax(0, 1fr);
      gap: 2.5rem;
    }
    .docs-sidebar {
      display: none; /* Hide sidebar on mobile to keep clean layout */
    }
  }
</style>
