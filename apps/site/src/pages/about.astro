---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About | Blood Kings Monitoring" description="The story and values behind the Blood Kings Monitoring project. Learn more about the developers and the open source MIT license.">
  <section class="page-header section">
    <div class="container text-center">
      <span class="badge badge-accent">Our Story</span>
      <h1>About Blood Kings</h1>
      <p class="section-subtitle">We build developer-focused monitoring tools without artificial tier limits or pricing subscriptions.</p>
    </div>
  </section>

  <!-- Story Section -->
  <section class="section bg-dim">
    <div class="container story-layout">
      <div class="story-text">
        <h2>How it Started</h2>
        <p>
          The **Blood Kings Monitoring** project was born out of frustration with the increasing costs and artificial limits imposed on free tiers by major cloud uptime checking providers. We believe that uptime tracking is a fundamental component of any modern application and shouldn't require paying subscription taxes for each added endpoint or telemetry node.
        </p>
        <p>
          We set out to build a platform that combines the configuration simplicity of cloud tools with the security and data isolation of self-hosted software. Our focus is to enable anyone to monitor their complete hardware and service footprint on their own servers.
        </p>
        
        <h3 class="margin-top">Core Values</h3>
        <ul class="values-list">
          <li><strong>Absolute Privacy:</strong> Your network ping data, system loads, and incident histories remain on your servers. We collect zero logs.</li>
          <li><strong>High Performance:</strong> Static layouts ensure fast delivery with minimal footprint on client browsers. Probing scripts run light.</li>
          <li><strong>Open Development:</strong> The codebase is 100% open-source under the MIT license, encouraging community audits.</li>
        </ul>
      </div>

      <!-- Quick stats column -->
      <div class="story-aside">
        <div class="card card-glow project-meta-box">
          <h4>Project Details</h4>
          <div class="meta-item">
            <span>License:</span>
            <strong>MIT License</strong>
          </div>
          <div class="meta-item">
            <span>Stack:</span>
            <strong>PHP / SQLite / JS</strong>
          </div>
          <div class="meta-item">
            <span>Latest Release:</span>
            <strong>v0.1.0-alpha</strong>
          </div>
          <div class="meta-item font-mono-link">
            <span>GitHub:</span>
            <strong><a href="https://github.com/BKPepe/bloodkings" target="_blank" rel="noopener">BKPepe/bloodkings</a></strong>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contributors Section -->
  <section class="section">
    <div class="container">
      <div class="section-header text-center">
        <h2>Developers & Contributors</h2>
        <p class="section-subtitle">Special thanks to everyone contributing to the Blood Kings project.</p>
      </div>

      <!-- Loading spinner -->
      <div id="contributors-loading" class="contributors-loading">
        <svg class="spinner" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="3"></circle></svg>
      </div>

      <!-- Contributors Grid -->
      <div class="contributors-grid" id="contributors-grid" style="display: none;">
        <!-- Hydrated dynamically -->
      </div>
    </div>
  </section>

  <script>
    async function loadContributors() {
      const loader = document.getElementById('contributors-loading');
      const grid = document.getElementById('contributors-grid');

      try {
        // Query GitHub API directly client-side
        const response = await fetch('https://api.github.com/repos/BKPepe/bloodkings/contributors');
        if (!response.ok) throw new Error('API failed');

        const contributors = await response.json();
        if (contributors && contributors.length > 0) {
          renderContributors(contributors);
          if (loader) loader.style.display = 'none';
          if (grid) grid.style.display = 'grid';
          return;
        }
        showFallbackContributors();
      } catch (e) {
        console.warn('Could not fetch contributors from GitHub, using static list.', e);
        showFallbackContributors();
      } finally {
        if (loader) loader.style.display = 'none';
        if (grid) grid.style.display = 'grid';
      }
    }

    function renderContributors(list: any[]) {
      const grid = document.getElementById('contributors-grid');
      if (!grid) return;

      grid.innerHTML = '';
      list.forEach((user: any) => {
        const userCard = document.createElement('a');
        userCard.href = user.html_url;
        userCard.target = '_blank';
        userCard.rel = 'noopener';
        userCard.className = 'contributor-card card';

        userCard.innerHTML = `
          <img src="${user.avatar_url}" alt="${user.login}" class="contrib-avatar" />
          <h5>${user.login}</h5>
          <span class="contrib-commits">${user.contributions} contributions</span>
        `;
        grid.appendChild(userCard);
      });
    }

    function showFallbackContributors() {
      const grid = document.getElementById('contributors-grid');
      if (!grid) return;

      grid.innerHTML = `
        <a href="https://github.com/BKPepe" target="_blank" rel="noopener" class="contributor-card card">
          <img src="https://github.com/BKPepe.png" alt="BKPepe" class="contrib-avatar" />
          <h5>BKPepe</h5>
          <span class="contrib-commits">Creator / Maintainer</span>
        </a>
      `;
    }

    loadContributors();
  </script>
</Layout>

<style>
  .page-header {
    background: radial-gradient(circle at 50% 100%, var(--bg-secondary) 0%, transparent 60%);
    border-bottom: 1px solid var(--border-color);
    padding: 6rem 0 4rem;
  }

  .page-header h1 {
    font-size: 3rem;
    font-weight: 800;
  }

  .story-layout {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4rem;
    align-items: flex-start;
  }

  .story-text {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .story-text h2 {
    font-size: 1.8rem;
    font-weight: 800;
  }

  .story-text p {
    font-size: 1.05rem;
    line-height: 1.7;
  }

  .margin-top {
    margin-top: 1.5rem;
  }

  .values-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .values-list li {
    position: relative;
    padding-left: 1.5rem;
    font-size: 1rem;
    color: var(--text-secondary);
  }

  .values-list li::before {
    content: '👑';
    position: absolute;
    left: 0;
    font-size: 0.95rem;
  }

  /* Aside details */
  .project-meta-box {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .project-meta-box h4 {
    font-size: 1.1rem;
    font-weight: 700;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 0.8rem;
  }

  .meta-item {
    display: flex;
    justify-content: space-between;
    font-size: 0.95rem;
  }

  .meta-item span {
    color: var(--text-muted);
  }

  .font-mono-link strong a {
    color: var(--accent-color);
  }

  /* Contributor styles */
  .contributors-loading {
    display: flex;
    justify-content: center;
    padding: 3rem 0;
  }

  .spinner {
    animation: spin 0.8s linear infinite;
    width: 32px;
    height: 32px;
    color: var(--accent-color);
  }

  .spinner circle {
    stroke-dasharray: 42, 38;
    stroke-linecap: round;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  .contributors-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .contributor-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.8rem;
    padding: 2rem 1.5rem;
  }

  .contrib-avatar {
    width: 70px;
    height: 70px;
    border-radius: 50%;
    border: 2px solid var(--border-color);
    transition: border-color var(--transition-fast);
  }

  .contributor-card:hover .contrib-avatar {
    border-color: var(--accent-color);
  }

  .contributor-card h5 {
    font-size: 1.05rem;
    font-weight: 700;
  }

  .contrib-commits {
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  @media (max-width: 992px) {
    .story-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .contributors-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 600px) {
    .contributors-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 400px) {
    .contributors-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
